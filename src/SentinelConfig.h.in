#ifndef SENTINEL_CONFIG_H
#define SENTINEL_CONFIG_H

#cmakedefine ASSERTIONS_ENABLED

#ifdef WIN32
	#define EXPORT __declspec(dllimport)
	#define IMPORT __declspec(dllimport)
#else
	#define EXPORT 
	#define IMPORT 
#endif

#endif